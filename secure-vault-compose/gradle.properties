# Per-module override for vanniktech-maven-publish. Inherits GROUP and
# VERSION_NAME from the root gradle.properties ? both artifacts ship on the
# same version line, tagged once with vX.Y.Z. Only the artifactId differs
# from the core ':secure-vault' module.

POM_ARTIFACT_ID=secure-vault-compose

